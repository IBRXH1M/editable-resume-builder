// src/types.ts
export interface ResumeSection {
  id: string;
  title: string;
  content: string;
}

export interface FormData {
  name: string;
  contact: string;
  profilePicture: string;
  education: string;
  skills: string;
  workExperience: string;
}
